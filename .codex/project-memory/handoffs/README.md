# Handoffs

Create one timestamped Markdown file per task transfer. Use the `handoff` command or copy the
template from `../templates/handoff.md`.

Do not rewrite another branch's handoff. Add a newer record that identifies the superseded record
and explains the verified change.
