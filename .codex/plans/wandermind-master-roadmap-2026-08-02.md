# WanderMind 主路线图与验收台账

更新时间：2026-08-16
状态：当前 PR 分支已通过本地与 CI 验证；生产仍是 `main@3fbf898` 的旧前端基线，发布后验收尚未完成
执行级别：critical / L3
视觉基线：沿用 ClaudeCode 已建立的 teal + gold 设计系统，巴厘岛为主，其他目的地保留并可扩展。

## 1. 已冻结的产品决策

| 决策 | 当前结论 | 验收口径 |
| --- | --- | --- |
| C1 管理员 | 独立管理员，显示名/用户名为 `admin`。本地开发可用 `123456`；生产密码必须由环境变量注入，不得硬编码进仓库或前端。 | 管理员不受路线收费限制；普通用户不能伪造管理员身份；生产环境缺少强密码时不自动创建弱管理员。 |
| C2 AI 自助规划额度 | 每个产品行程提供 1 次初始 AI 规划 + 2 次 AI 调整；公共路线无需登录即可完整浏览。 | AI 自助额度按行程计数，并与专业路线权益完全独立；不得通过普通生成接口绕过计数。 |
| C3 推荐积分 | 邀请人 +10，被邀请人 +5；30 分可兑换 1 次自动专业路线；注册后 24 小时进入可用状态；每位邀请人每月最多计 5 个有效邀请。 | 防止自邀、重复奖励与立即刷分；积分不可兑换人工定制或司机费用。 |
| C4 专业路线解锁 | 个性化专业路线先开放约 70%；¥9.9 人工确认或 30 推荐积分解锁同一个 `professional_route_entitlement`，并包含同一行程 3 次参数调整。MVP 使用二维码付款 + 管理员人工确认。 | 7 天显示 5 天、5 天至少锁 1 天、3 天显示 2 天；历史已付费权益按已存额度兼容，不降低既有用户权益。允许调整日期/天数、节奏、预算、兴趣、风格和人数，不等于新建 3 个订单或人工深度定制。 |
| 路线体系 | G1–G7 是后台地理事实层；R1–R6 是前台主题产品层。 | 页面不把后台复杂分类直接压给用户；路线必须能追溯到区域、节点和 POI。 |
| 司机报价 | 已收到公开参考基线：全天 IDR 700k/10h、半天 IDR 500k/6h、每位游客 +IDR 50k、全天超过 10h 后 +IDR 75k/h；机场接送、换酒店和路线附加费按距离与实际行程确认。 | 页面必须标注参考价与最终确认边界；逐司机适用范围、佩妮达船车衔接和具体路线总价仍由司机确认。 |
| 图片版权 | 用户已于 2026-08-05 统一确认当前候选图片的人物授权与公开使用许可；水印、损坏、重复、敏感信息或来源异常文件仍自动排除。 | 108 张唯一素材均有哈希、Web 优化副本和发布 manifest；公开发布仍须具备完整 metadata、替代文本与正确地点/主题归类。 |

## 2. 圆周旅迹参考：学什么，不学什么

参考来源：

- 官方 App Store 产品页：导入攻略、AI 行程、地图路线、天气交通、多人协作和附近地点。
- 官方行程指南：需求输入 → AI 框架 → 导入收藏内容 → 抽取 POI → 拖入天数 → 地理与交通优化。
- 官方产品截图：移动端地图作为上下文，日程卡片/天数标签作为主要操作面；地点收藏使用地图 + 底部卡片。

### 可迁移的构建方式

1. 用明确任务流替代空白聊天框。
2. 先给完整粗路线建立信任，再出售核验和落地细节。
3. 让用户按“天”编辑、增删和调整地点，不必每次整份重生成。
4. 地图与日程同步高亮，地图用于理解空间关系，不承担全部文字。
5. 把收藏内容抽成结构化 POI；WanderMind 首期先用自己的巴厘岛资料库，导入外部攻略列为后续功能。
6. 移动端采用单列、底部操作区和渐进展开，避免把桌面大面板硬压进小屏幕。

### 必须规避

1. 地图标签拥挤和信息过载。
2. 外部攻略抽取错误、地点过期却不标注核验状态。
3. 只生成通用路线，没有本地可执行细节。
4. 复制其品牌、颜色、组件或宣传素材。
5. 在版权与解析规则未完成前贸然开放全网攻略导入。

WanderMind 的差异化不是“也有一个 AI 聊天框”，而是：

> 巴厘岛专业路线事实库 + 独立 AI 自助规划 + 已核验执行细节 + 真实本地司机交接。

## 3. 旧计划尚未闭环

| 优先级 | 项目 | 当前状态 | 完成效果 / 验收入口 |
| --- | --- | --- | --- |
| P0 | 图片分类、版权清单与真实推广文案 | 授权与处理闭环，25 张资料完成 | 108 张唯一候选素材均已统一授权、计算哈希、生成 Web 优化副本并进入发布 manifest；Portfolio 采用 Landscapes / Culture / Experiences 三类，Driver Moments 独立承接司机与游客合照。当前 25 张 D8 图片已补齐中/英/日/韩/印标题、说明与替代文本；`Nyepi.jpg` 已纠正为塞米亚克村社神庙的社区文化聚会；`bali-1.jpg` 因没有可见或 EXIF 地点证据，已移除 Bali 标签并降为未知地点的海岸场景。剩余 27 张继续按地点证据和发布门禁分批处理。 |
| P0 | 图片点击后显示景点目的、适合人群、简易路线和联系入口 | 结构化交接完成 | 15 张现有作品均可打开“画面之外的现实、适合目的、简易路线、关联 R1–R6”；关联路线能准确激活，地点 + `route_id` 可分别带入 AI 提示和司机询价表；后续新增照片仍须先过版权清单。 |
| P0 | 任意目的地 AI 草稿的事实核验与结构化增强 | 外部依赖 | 四个预设目的地五语资料已静态直出且不依赖模型；任意目的地草稿仍需登录及有效上游模型额度。 |
| P1 | 自动支付 | 未开始 | MVP 先采用二维码 + 管理员确认；自动支付延期。 |
| P1 | 巴厘岛内容持续扩充 | 单图闭环持续推进 | 已建立图片收件工具与权利门禁；重复运行收件工具会按 SHA-256 保留已审核的 manifest 版本、批准记录、Web 路径和扩展语言资料。管理员动态链路已完成首图生产生命周期；25 张图片完成五语言资料，最新单元将 `bali-1.jpg` 从文件名推断修正为未知地点海岸场景，剩余 27 张按同一门禁逐图审核。 |
| P2 | 其他目的地作品集 | 未开始 | 京都、巴黎、圣托里尼继续保留入口，但不稀释巴厘岛主线。 |
| P2 | `dest_info` 预加载/缓存 | 已完成 | 巴厘岛/京都/巴黎/圣托里尼采用版本化五语精选数据静态直出；任意目的地按城市和语言隔离缓存；实时天气保持独立。 |
| P3 | 小程序/原生 App | 未开始 | 当前先把响应式 Web 和 Safari/WebKit 体验做稳。 |

## 4. 本次 1–11 计划

| 编号 | 优先级 | 项目 | 当前状态 | 目标效果 |
| --- | --- | --- | --- | --- |
| 1 | P1 | 作品集移动到推荐路线之后 | 已完成 | DOM 运行顺序为路线家族 → 示例行程 → 作品集；桌面与手机一致。 |
| 2 | P0 | 多路线家族与路线视觉化 | 增强版完成 | R1–R6 卡片、G1–G7 路径和三天免费轮廓已上线；可调整天序、添加/移除同区域 POI，日程与地图同步高亮并保存本机。 |
| 3 | P1 | 首页主次目的地与 Hero 文案 | 已完成 | 巴厘岛为默认主入口；京都、巴黎、圣托里尼和任意目的地均保留。 |
| 4 | P0 | 首页需求表单 | 已完成 | 人数、日期、预算、语言默认币种、手动币种、多选动机与免费/专业双入口齐全。 |
| 5 | P0 | 用户画像跨页带入 | 已完成 | `wm_studio_trip_brief` 在首页、巴厘岛、AI 和司机页复用；路线与司机页自动预填。 |
| 6 | P0 | 多选旅行动机与路线匹配 | 第一版完成 | 首访→R1、在地文化→R4、摄影→R6；其余意图可在路线卡继续选择。 |
| 7 | P1 | 作品集分类与标签 | D8 当前版完成 | Portfolio 统一为 Landscapes / Culture / Experiences 三类；Places 拆为 `region`、`area`、`placeName`、`placeType`、`prominence`、`routeIds`、`tags`、`mood`、`photographyStyle` 和 `verificationStatus`；司机游客合照主要进入 Driver Moments。 |
| 8 | P0 | G1–G7、节点、POI、店铺和体验数据底座 | 第一版持续扩充 | 分支 JSON 含 7 区、6 路线、62 POI：56 个 `verified`、3 个 `pending_review`、3 个 `needs_supplier_confirmation`；新增 Taman Ayun、Taman Saraswati 与 Sundays Beach Club，动态票务、仪式、潮汐和活动仍实时复核。生产仍是 50 POI 的旧基线。 |
| 9 | P0 | AI 自助规划、专业路线预览/解锁与司机交接 | 分支 MVP 完成，待生产验收 | AI 自助为每行程 1 次初始规划 + 2 次调整；专业路线约 70% 预览，¥9.9 与 30 积分解锁同一权益并含 3 次调整；二维码订单、管理员确认和司机路线带入已闭环。 |
| 10 | P1 | Dicky / Gede 双司机资料与车辆选择 | 第一版完成 | 两位司机完整自我介绍、车辆与 Driver Moments 已接入；用户只通过站内隐私表单联系，网站不公开个人邮箱、微信或 WhatsApp；公开价格使用已确认参考基线，逐司机路线总价仍待确认。 |
| 11 | P0 | 顾问框架与产品决策门禁 | 已完成 | 项目本地主题 Skill 固化产品、商业、运营边界；人物视角不能替代 WanderMind 设计规范。 |

## 5. 分阶段实施与验证

### Phase P0-A：事实与产品数据底座

1. 建 G1–G7、R1–R6、POI、体验、图片权利清单。
   验证：JSON 结构校验通过；每条路线能追溯到区域与 POI。
2. 建图片收件工具，自动读取文件名、尺寸、哈希、候选分类、地点状态和路线/POI 建议。
   验证：不移动/覆盖原图；重跑和唯一哈希改名不丢人工字段；重复副本不继承批准；只有人工确认、授权状态合格且来源完整的图片进入发布清单。
3. 把用户画像、币种和路线选择定义成统一 trip brief。
   验证：首页、AI、巴厘岛、司机页读取同一 schema。

### Phase P0-B：账户、额度与商业闭环

1. 独立管理员与角色校验。
   验证：本地可测试；生产不接受仓库内弱密码；普通用户越权返回 403。
2. AI 自助规划按每个产品行程提供 1 次初始规划 + 2 次调整；专业路线另有同一行程 3 次参数调整。
   验证：两类额度独立按 trip/entitlement 计数；管理员权限不允许普通用户伪造；专业路线第 4 次调整被拒绝，普通生成接口不能绕过扣次。
3. 推荐积分与兑换。
   验证：自邀/重复/月上限/24h pending/幂等均有测试。
4. ¥9.9 订单和管理员确认。
   验证：支付前 locked，确认后 unlocked，重复确认不重复记账。

### Phase P0-C：巴厘岛路线产品

1. R1–R6 路线卡片、动机匹配、按天预览与天序调整。
2. 地图/区域节点联动、同区域 POI 增删和所选日期同步高亮。
3. 公共路线完整免费浏览、AI 自助规划与约 70% 专业路线预览的清晰差异。
4. 一键把已编辑路线保存为结构化草稿，并导入 AI 或 Dicky/Gede 询价表。

验证：桌面 1440/1280、平板 768、手机 430/390/375/360/320；Chrome、Edge、WebKit；键盘、触屏、深色模式、减少动画和五语言。

### Phase P1：内容、司机与作品集

1. 作品集移位、D8 三类主题和 Places 结构化字段。
2. 商家/体验卡片只使用可授权图片与官方预约链接。
3. Dicky/Gede 资料和车辆补齐。
4. README、部署变量、验收证据和回退说明同步。

## 6. 非目标与风险

- 本轮不承诺自动支付、原生 App、全网攻略导入或完整全球目的地内容。
- 不根据模型猜测司机价格、营业时间、评分、医学功效或商家授权。
- Render/Neon/模型供应商属于外部系统；生产变更必须在本地测试通过后独立验收。
- WebKit 自动化不能完全替代实体 iPhone Safari，最终保留真机抽查门禁。
